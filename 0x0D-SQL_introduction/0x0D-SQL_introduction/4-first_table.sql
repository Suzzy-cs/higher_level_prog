-- create table first_table with id INT and name VARCHAR
-- If the table first_table already exists, your script should not fail
CREATE TABLE IF NOT EXISTS `first_table` (
	`id` INT,
	`name` VARCHAR(256)
);
